# 🚀 Быстрый старт для тестирования

## 1. Подготовка базы данных

```bash
cd server

# Установите PostgreSQL и создайте базу данных
createdb staff_notify  # или используйте pgAdmin/другой инструмент

# Создайте .env файл в папке server с содержимым:
# DATABASE_URL="postgresql://username:password@localhost:5432/staff_notify?schema=public"
# JWT_SECRET="your-secret-key-here-change-in-production"
# PORT=3000

# Примените миграции
pnpm prisma:migrate

# Заполните базу тестовыми данными
pnpm seed
```

## 2. Запуск сервера

```bash
cd server
pnpm dev
```

✅ **Проверьте:** http://localhost:3000/health возвращает `{"status":"ok","time":"..."}`

## 3. Запуск мобильного приложения

```bash
cd mobile

# Установите зависимости (если еще не установлены)
npm install

# Обновите IP адрес в mobile/lib/api.ts
# Измените строку 4: API_BASE на IP вашего компьютера
# Например: "http://192.168.1.6:3000"

npm start
```

## 4. Тестовые аккаунты

### Админ
- Email: `admin@corp.local`
- Пароль: `Admin123!`
- Роль: admin
- Отдел: IT

### Сотрудники (любой из них)
- Email: `alice.it@corp.local`, `kate.sales@corp.local`, etc.
- Пароль: `User123!`
- Роль: employee

## 5. Быстрый тест функциональности

### Тест 1: Вход и просмотр ленты
1. Откройте приложение
2. Войдите как админ
3. Убедитесь, что видите пустую ленту или имеющиеся объявления

### Тест 2: Создание объявления
1. Нажмите "Создать"
2. Заполните заголовок и текст
3. Выберите отдел IT
4. Опубликуйте

### Тест 3: Просмотр статистики
1. Откройте только что созданное объявление
2. Нажмите "Статистика прочтений"
3. Убедитесь, что видите 0% (0 читателей)
4. Вернитесь и нажмите "Отметить прочитанным"
5. Снова откройте статистику - должно быть 100%

### Тест 4: Вход другим пользователем
1. Выйдите из аккаунта
2. Войдите как `alice.it@corp.local`
3. Убедитесь, что видите объявление созданное в тесте 2
4. Откройте его и отметьте прочитанным

### Тест 5: Проверка статистики с нескольких пользователей
1. Войдите как админ
2. Откройте объявление и статистику
3. Должно быть больше 0% если алиса прочитала
4. Закройте приложение
5. Откройте на другом устройстве (или эмуляторе)
6. Войдите как `bob.it@corp.local`
7. Откройте то же объявление и отметьте прочитанным
8. Вернитесь к админу - обновите статистику
9. Должно быть больше %

## 6. Полный чек-лист

Откройте файл `TESTING_CHECKLIST.md` для полного списка тестов

## 7. Troubleshooting

### Проблема: Не могу подключиться к серверу
**Решение:** Проверьте:
- Сервер запущен на порту 3000
- IP адрес в `mobile/lib/api.ts` правильный
- Файерволл не блокирует порт 3000
- Устройство и сервер в одной сети WiFi

### Проблема: База данных не подключается
**Решение:** Проверьте:
- PostgreSQL запущен
- DATABASE_URL в .env правильный
- База данных существует

### Проблема: Приложение не запускается
**Решение:** Выполните:
```bash
cd mobile
rm -rf node_modules
npm install
npm start
```

### Проблема: "Unauthorized" ошибки
**Решение:** 
- Выйдите из приложения
- Удалите кэш: `Settings > App > Clear Data`
- Запустите снова

## 📞 Нужна помощь?

Проверьте логи:
- Сервер: терминал где запущен `pnpm dev`
- Мобильное: Metro Bundler консоль
- React Native: `npx react-native log-android` или `npx react-native log-ios`

---

**Удачи в тестировании! 🎉**

